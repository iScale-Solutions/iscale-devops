  # AppServerLaunchConfig: 
  #   Type: AWS::AutoScaling::LaunchConfiguration
  #   Metadata: 
  #     AWS::CloudFormation::Init: 
  #       configSets: 
  #         MyConfigset: 
  #           - InstallHardening
        # InstallHardening:
          packages:
            yum: 
              git: []
          commands:
            command01:
              command: git clone https://github.com/nozaq/amazon-linux-cis.git
              cwd: /root
            command02:
              command: python ./amazon-linux-cis --disable-mount-options -v
              cwd: /root