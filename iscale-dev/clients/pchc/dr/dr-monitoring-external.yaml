AWSTemplateFormatVersion: 2010-09-09
Description: DR Monitoring stack — Route 53 health checks, Synthetics canaries, and AWS Health EventBridge for all 10 PCHC apps (us-east-1)

Metadata:
  AWS::CloudFormation::Interface:
    ParameterGroups:
      - Label:
          default: Resource Configuration
        Parameters:
          - Name
          - OrgName
          - EnvName
      - Label:
          default: Stack Configuration
        Parameters:
          - SNSStackName
      - Label:
          default: Monitoring Configuration
        Parameters:
          - HealthCheckInterval
          - HealthCheckFailureThreshold
          - CanarySchedule
          - LogRetentionDays
      - Label:
          default: App Endpoints
        Parameters:
          - PesonetEndpoint
          - PddtsEndpoint
          - PddtsEodEndpoint
          - MisEndpoint
          - CicsEndpoint
          - DirectDebitEndpoint
          - Pas6Endpoint
          - DsappEndpoint
          - PaygateEndpoint
          - PhilpassEndpoint
      - Label:
          default: Health Check Paths
        Parameters:
          - PesonetHealthCheckPath
          - PddtsHealthCheckPath
          - PddtsEodHealthCheckPath
          - MisHealthCheckPath
          - CicsHealthCheckPath
          - DirectDebitHealthCheckPath
          - Pas6HealthCheckPath
          - DsappHealthCheckPath
          - PaygateHealthCheckPath
          - PhilpassHealthCheckPath

    ParameterLabels:
      Name:
        default: Resource Name
      OrgName:
        default: Organization Name
      EnvName:
        default: Environment Name
      SNSStackName:
        default: SNS Topic Stack Name
      HealthCheckInterval:
        default: Health Check Interval (seconds)
      HealthCheckFailureThreshold:
        default: Health Check Failure Threshold
      CanarySchedule:
        default: Canary Schedule Expression
      LogRetentionDays:
        default: Canary Log & Artifact Retention (days)
      PesonetEndpoint:
        default: PESONET Endpoint
      PddtsEndpoint:
        default: PDDTS Endpoint
      PddtsEodEndpoint:
        default: PDDTS-EOD Endpoint
      MisEndpoint:
        default: MIS Endpoint
      CicsEndpoint:
        default: CICS Endpoint
      DirectDebitEndpoint:
        default: DirectDebit Endpoint
      Pas6Endpoint:
        default: PAS6 Endpoint
      DsappEndpoint:
        default: DSAPP Endpoint
      PaygateEndpoint:
        default: PayGate Endpoint
      PhilpassEndpoint:
        default: PhilPass Endpoint
      PesonetHealthCheckPath:
        default: PESONET Health Check Path
      PddtsHealthCheckPath:
        default: PDDTS Health Check Path
      PddtsEodHealthCheckPath:
        default: PDDTS-EOD Health Check Path
      MisHealthCheckPath:
        default: MIS Health Check Path
      CicsHealthCheckPath:
        default: CICS Health Check Path
      DirectDebitHealthCheckPath:
        default: DirectDebit Health Check Path
      Pas6HealthCheckPath:
        default: PAS6 Health Check Path
      DsappHealthCheckPath:
        default: DSAPP Health Check Path
      PaygateHealthCheckPath:
        default: PayGate Health Check Path
      PhilpassHealthCheckPath:
        default: PhilPass Health Check Path

Parameters:

  # Resource Configuration
  Name:
    Description: Unique name/identifier for this stack's resources
    Type: String
    Default: dr-monitoring
    MinLength: 3
    MaxLength: 20
  OrgName:
    Description: The name of the organization
    Type: String
    Default: pchc
    AllowedPattern: '^[a-z]+(-[a-z]+)*$'
    ConstraintDescription: Must only contain lowercase letters and hyphens
    MaxLength: 10
  EnvName:
    Description: The name of the environment
    Type: String
    Default: Production
    AllowedValues:
      - Production
      - Staging
      - Development
      - Test

  # Stack Configuration
  SNSStackName:
    Description: The name of the SNS stack
    Type: String
    Default: pchc-prd-ue1-sns
    MinLength: 1

  # Monitoring Configuration
  HealthCheckInterval:
    Description: Route 53 health check polling interval in seconds
    Type: Number
    Default: 10
    AllowedValues:
      - 10
      - 30
  HealthCheckFailureThreshold:
    Description: Consecutive failures before Route 53 marks a health check unhealthy
    Type: Number
    Default: 2
    MinValue: 1
    MaxValue: 10
  CanarySchedule:
    Description: Synthetics canary schedule expression
    Type: String
    Default: rate(5 minutes)
  LogRetentionDays:
    Description: Retention period for Synthetics canary log groups and S3 artifacts (days)
    Type: Number
    Default: 30
    AllowedValues:
      - 7
      - 14
      - 30
      - 60
      - 90

  # App Endpoints
  PesonetEndpoint:
    Description: PESONET application domain (no https://)
    Type: String
    Default: pesonet.pchc.com.ph
  PddtsEndpoint:
    Description: PDDTS application domain (no https://)
    Type: String
    Default: pddts.pchc.com.ph
  PddtsEodEndpoint:
    Description: PDDTS-EOD application domain (no https://)
    Type: String
    Default: pddts-eod.pchc.com.ph
  MisEndpoint:
    Description: MIS application domain (no https://)
    Type: String
    Default: mis.pchc.com.ph
  CicsEndpoint:
    Description: CICS application domain (no https://)
    Type: String
    Default: cics.pchc.com.ph
  DirectDebitEndpoint:
    Description: DirectDebit application domain (no https://)
    Type: String
    Default: directdebit.pchc.com.ph
  Pas6Endpoint:
    Description: PAS6 application domain (no https://)
    Type: String
    Default: pas6.pchc.com.ph
  DsappEndpoint:
    Description: DSAPP application domain (no https://)
    Type: String
    Default: dsapp.pchc.com.ph
  PaygateEndpoint:
    Description: PayGate application domain (no https://)
    Type: String
    Default: paygate.pchc.com.ph
  PhilpassEndpoint:
    Description: PhilPass application domain (no https://)
    Type: String
    Default: philpass.pchc.com.ph

  # Health Check Paths
  PesonetHealthCheckPath:
    Description: URL path for PESONET health check
    Type: String
    Default: /
  PddtsHealthCheckPath:
    Description: URL path for PDDTS health check
    Type: String
    Default: /
  PddtsEodHealthCheckPath:
    Description: URL path for PDDTS-EOD health check
    Type: String
    Default: /
  MisHealthCheckPath:
    Description: URL path for MIS health check
    Type: String
    Default: /
  CicsHealthCheckPath:
    Description: URL path for CICS health check
    Type: String
    Default: /
  DirectDebitHealthCheckPath:
    Description: URL path for DirectDebit health check
    Type: String
    Default: /
  Pas6HealthCheckPath:
    Description: URL path for PAS6 health check
    Type: String
    Default: /
  DsappHealthCheckPath:
    Description: URL path for DSAPP health check
    Type: String
    Default: /
  PaygateHealthCheckPath:
    Description: URL path for PayGate health check
    Type: String
    Default: /
  PhilpassHealthCheckPath:
    Description: URL path for PhilPass health check
    Type: String
    Default: /

Resources:

##
# Shared — Synthetics
##

  CanaryArtifactBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub '${OrgName}-dr-monitoring-canary-${AWS::AccountId}'
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      LifecycleConfiguration:
        Rules:
          - Id: ExpireCanaryArtifacts
            Status: Enabled
            ExpirationInDays: !Ref LogRetentionDays
      Tags:
        - Key: Name
          Value: !Sub '${OrgName}-dr-monitoring-canary-${AWS::AccountId}'
        - Key: Organization
          Value: !Ref OrgName
        - Key: Environment
          Value: !Ref EnvName

  CanaryArtifactBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref CanaryArtifactBucket
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Sid: AllowCanaryRoleObjects
            Effect: Allow
            Principal:
              AWS: !GetAtt CanaryExecutionRole.Arn
            Action:
              - s3:PutObject
              - s3:GetObject
            Resource: !Sub 'arn:aws:s3:::${CanaryArtifactBucket}/*'
          - Sid: AllowCanaryRoleBucket
            Effect: Allow
            Principal:
              AWS: !GetAtt CanaryExecutionRole.Arn
            Action:
              - s3:GetBucketLocation
              - s3:ListBucket
            Resource: !Sub 'arn:aws:s3:::${CanaryArtifactBucket}'

  CanaryExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub '${OrgName}-dr-monitoring-canary-role'
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: CanaryExecutionPolicy
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - s3:PutObject
                  - s3:GetObject
                  - s3:GetBucketLocation
                  - s3:ListBucket
                Resource:
                  - !Sub 'arn:aws:s3:::${CanaryArtifactBucket}'
                  - !Sub 'arn:aws:s3:::${CanaryArtifactBucket}/*'
              - Effect: Allow
                Action:
                  - cloudwatch:PutMetricData
                Resource: '*'
                Condition:
                  StringEquals:
                    cloudwatch:namespace: CloudWatchSynthetics
              - Effect: Allow
                Action:
                  - logs:CreateLogGroup
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                Resource: !Sub 'arn:aws:logs:${AWS::Region}:${AWS::AccountId}:log-group:/aws/synthetics/*'
      Tags:
        - Key: Name
          Value: !Sub '${OrgName}-dr-monitoring-canary-role'
        - Key: Organization
          Value: !Ref OrgName
        - Key: Environment
          Value: !Ref EnvName

##
# Shared — AWS Health EventBridge
##

  AWSHealthEventRule:
    Type: AWS::Events::Rule
    Properties:
      Name: !Sub '${OrgName}-prd-ue1-dr-monitoring-health-rule'
      Description: AWS Health events for ap-southeast-1 service disruptions
      State: ENABLED
      EventPattern:
        source:
          - aws.health
        detail-type:
          - AWS Health Event
        detail:
          eventScopeCode:
            - PUBLIC
          affectedEntities:
            awsRegion:
              - ap-southeast-1
      Targets:
        - Id: DRMonitoringTopicTarget
          Arn:
            Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

##
# PESONET
##

  PesonetHealthCheck:
    Type: AWS::Route53::HealthCheck
    Properties:
      HealthCheckConfig:
        Type: HTTPS
        FullyQualifiedDomainName: !Ref PesonetEndpoint
        ResourcePath: !Ref PesonetHealthCheckPath
        RequestInterval: !Ref HealthCheckInterval
        FailureThreshold: !Ref HealthCheckFailureThreshold
        EnableSNI: true
      HealthCheckTags:
        - Key: Name
          Value: !Sub '${OrgName}-pesonet-health-check'
        - Key: Organization
          Value: !Ref OrgName
        - Key: Environment
          Value: !Ref EnvName

  PesonetHealthCheckAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub '${OrgName}-pesonet-r53-alarm'
      AlarmDescription: PESONET Route 53 health check is failing
      Namespace: AWS/Route53
      MetricName: HealthCheckStatus
      Dimensions:
        - Name: HealthCheckId
          Value: !GetAtt PesonetHealthCheck.HealthCheckId
      Statistic: Minimum
      Period: 60
      EvaluationPeriods: !Ref HealthCheckFailureThreshold
      Threshold: 1
      ComparisonOperator: LessThanThreshold
      TreatMissingData: breaching
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

  PesonetCanaryLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/aws/synthetics/${OrgName}-pesonet'
      RetentionInDays: !Ref LogRetentionDays

  PesonetCanary:
    Type: AWS::Synthetics::Canary
    DependsOn: PesonetCanaryLogGroup
    Properties:
      Name: !Sub '${OrgName}-pesonet'
      ArtifactS3Location: !Sub 's3://${CanaryArtifactBucket}/${OrgName}/pesonet'
      ExecutionRoleArn: !GetAtt CanaryExecutionRole.Arn
      RuntimeVersion: syn-nodejs-puppeteer-9.1
      Schedule:
        Expression: !Ref CanarySchedule
      RunConfig:
        TimeoutInSeconds: 60
        EnvironmentVariables:
          ENDPOINT: !Ref PesonetEndpoint
          HEALTH_PATH: !Ref PesonetHealthCheckPath
      StartCanaryAfterCreation: true
      DeleteLambdaResourcesOnCanaryDeletion: true
      Code:
        Handler: index.handler
        Script: |
          const synthetics = require('Synthetics');
          exports.handler = async () => {
              await synthetics.executeHttpStep(
                  'Heartbeat',
                  'https://' + process.env.ENDPOINT + process.env.HEALTH_PATH
              );
          };
      Tags:
        - Key: Name
          Value: !Sub '${OrgName}-pesonet'
        - Key: Organization
          Value: !Ref OrgName
        - Key: Environment
          Value: !Ref EnvName

  PesonetCanaryAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub '${OrgName}-pesonet-canary-alarm'
      AlarmDescription: PESONET Synthetics canary heartbeat failed
      Namespace: CloudWatchSynthetics
      MetricName: SuccessPercent
      Dimensions:
        - Name: CanaryName
          Value: !Ref PesonetCanary
      Statistic: Average
      Period: 300
      EvaluationPeriods: 1
      Threshold: 100
      ComparisonOperator: LessThanThreshold
      TreatMissingData: breaching
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

  PesonetCompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub '${OrgName}-pesonet-external-composite-alarm'
      AlarmDescription: PESONET is unreachable — both Route 53 health check and Synthetics canary are failing
      AlarmRule: !Sub 'ALARM(${PesonetHealthCheckAlarm}) AND ALARM(${PesonetCanaryAlarm})'
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

##
# PDDTS
##

  PddtsHealthCheck:
    Type: AWS::Route53::HealthCheck
    Properties:
      HealthCheckConfig:
        Type: HTTPS
        FullyQualifiedDomainName: !Ref PddtsEndpoint
        ResourcePath: !Ref PddtsHealthCheckPath
        RequestInterval: !Ref HealthCheckInterval
        FailureThreshold: !Ref HealthCheckFailureThreshold
        EnableSNI: true
      HealthCheckTags:
        - Key: Name
          Value: !Sub '${OrgName}-pddts-health-check'
        - Key: Organization
          Value: !Ref OrgName
        - Key: Environment
          Value: !Ref EnvName

  PddtsHealthCheckAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub '${OrgName}-pddts-r53-alarm'
      AlarmDescription: PDDTS Route 53 health check is failing
      Namespace: AWS/Route53
      MetricName: HealthCheckStatus
      Dimensions:
        - Name: HealthCheckId
          Value: !GetAtt PddtsHealthCheck.HealthCheckId
      Statistic: Minimum
      Period: 60
      EvaluationPeriods: !Ref HealthCheckFailureThreshold
      Threshold: 1
      ComparisonOperator: LessThanThreshold
      TreatMissingData: breaching
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

  PddtsCanaryLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/aws/synthetics/${OrgName}-pddts'
      RetentionInDays: !Ref LogRetentionDays

  PddtsCanary:
    Type: AWS::Synthetics::Canary
    DependsOn: PddtsCanaryLogGroup
    Properties:
      Name: !Sub '${OrgName}-pddts'
      ArtifactS3Location: !Sub 's3://${CanaryArtifactBucket}/${OrgName}/pddts'
      ExecutionRoleArn: !GetAtt CanaryExecutionRole.Arn
      RuntimeVersion: syn-nodejs-puppeteer-9.1
      Schedule:
        Expression: !Ref CanarySchedule
      RunConfig:
        TimeoutInSeconds: 60
        EnvironmentVariables:
          ENDPOINT: !Ref PddtsEndpoint
          HEALTH_PATH: !Ref PddtsHealthCheckPath
      StartCanaryAfterCreation: true
      DeleteLambdaResourcesOnCanaryDeletion: true
      Code:
        Handler: index.handler
        Script: |
          const synthetics = require('Synthetics');
          exports.handler = async () => {
              await synthetics.executeHttpStep(
                  'Heartbeat',
                  'https://' + process.env.ENDPOINT + process.env.HEALTH_PATH
              );
          };
      Tags:
        - Key: Name
          Value: !Sub '${OrgName}-pddts'
        - Key: Organization
          Value: !Ref OrgName
        - Key: Environment
          Value: !Ref EnvName

  PddtsCanaryAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub '${OrgName}-pddts-canary-alarm'
      AlarmDescription: PDDTS Synthetics canary heartbeat failed
      Namespace: CloudWatchSynthetics
      MetricName: SuccessPercent
      Dimensions:
        - Name: CanaryName
          Value: !Ref PddtsCanary
      Statistic: Average
      Period: 300
      EvaluationPeriods: 1
      Threshold: 100
      ComparisonOperator: LessThanThreshold
      TreatMissingData: breaching
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

  PddtsCompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub '${OrgName}-pddts-external-composite-alarm'
      AlarmDescription: PDDTS is unreachable — both Route 53 health check and Synthetics canary are failing
      AlarmRule: !Sub 'ALARM(${PddtsHealthCheckAlarm}) AND ALARM(${PddtsCanaryAlarm})'
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

##
# PDDTS-EOD
##

  PddtsEodHealthCheck:
    Type: AWS::Route53::HealthCheck
    Properties:
      HealthCheckConfig:
        Type: HTTPS
        FullyQualifiedDomainName: !Ref PddtsEodEndpoint
        ResourcePath: !Ref PddtsEodHealthCheckPath
        RequestInterval: !Ref HealthCheckInterval
        FailureThreshold: !Ref HealthCheckFailureThreshold
        EnableSNI: true
      HealthCheckTags:
        - Key: Name
          Value: !Sub '${OrgName}-pddts-eod-health-check'
        - Key: Organization
          Value: !Ref OrgName
        - Key: Environment
          Value: !Ref EnvName

  PddtsEodHealthCheckAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub '${OrgName}-pddts-eod-r53-alarm'
      AlarmDescription: PDDTS-EOD Route 53 health check is failing
      Namespace: AWS/Route53
      MetricName: HealthCheckStatus
      Dimensions:
        - Name: HealthCheckId
          Value: !GetAtt PddtsEodHealthCheck.HealthCheckId
      Statistic: Minimum
      Period: 60
      EvaluationPeriods: !Ref HealthCheckFailureThreshold
      Threshold: 1
      ComparisonOperator: LessThanThreshold
      TreatMissingData: breaching
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

  PddtsEodCanaryLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/aws/synthetics/${OrgName}-pddts-eod'
      RetentionInDays: !Ref LogRetentionDays

  PddtsEodCanary:
    Type: AWS::Synthetics::Canary
    DependsOn: PddtsEodCanaryLogGroup
    Properties:
      Name: !Sub '${OrgName}-pddts-eod'
      ArtifactS3Location: !Sub 's3://${CanaryArtifactBucket}/${OrgName}/pddts-eod'
      ExecutionRoleArn: !GetAtt CanaryExecutionRole.Arn
      RuntimeVersion: syn-nodejs-puppeteer-9.1
      Schedule:
        Expression: !Ref CanarySchedule
      RunConfig:
        TimeoutInSeconds: 60
        EnvironmentVariables:
          ENDPOINT: !Ref PddtsEodEndpoint
          HEALTH_PATH: !Ref PddtsEodHealthCheckPath
      StartCanaryAfterCreation: true
      DeleteLambdaResourcesOnCanaryDeletion: true
      Code:
        Handler: index.handler
        Script: |
          const synthetics = require('Synthetics');
          exports.handler = async () => {
              await synthetics.executeHttpStep(
                  'Heartbeat',
                  'https://' + process.env.ENDPOINT + process.env.HEALTH_PATH
              );
          };
      Tags:
        - Key: Name
          Value: !Sub '${OrgName}-pddts-eod'
        - Key: Organization
          Value: !Ref OrgName
        - Key: Environment
          Value: !Ref EnvName

  PddtsEodCanaryAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub '${OrgName}-pddts-eod-canary-alarm'
      AlarmDescription: PDDTS-EOD Synthetics canary heartbeat failed
      Namespace: CloudWatchSynthetics
      MetricName: SuccessPercent
      Dimensions:
        - Name: CanaryName
          Value: !Ref PddtsEodCanary
      Statistic: Average
      Period: 300
      EvaluationPeriods: 1
      Threshold: 100
      ComparisonOperator: LessThanThreshold
      TreatMissingData: breaching
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

  PddtsEodCompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub '${OrgName}-pddts-eod-external-composite-alarm'
      AlarmDescription: PDDTS-EOD is unreachable — both Route 53 health check and Synthetics canary are failing
      AlarmRule: !Sub 'ALARM(${PddtsEodHealthCheckAlarm}) AND ALARM(${PddtsEodCanaryAlarm})'
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

##
# MIS
##

  MisHealthCheck:
    Type: AWS::Route53::HealthCheck
    Properties:
      HealthCheckConfig:
        Type: HTTPS
        FullyQualifiedDomainName: !Ref MisEndpoint
        ResourcePath: !Ref MisHealthCheckPath
        RequestInterval: !Ref HealthCheckInterval
        FailureThreshold: !Ref HealthCheckFailureThreshold
        EnableSNI: true
      HealthCheckTags:
        - Key: Name
          Value: !Sub '${OrgName}-mis-health-check'
        - Key: Organization
          Value: !Ref OrgName
        - Key: Environment
          Value: !Ref EnvName

  MisHealthCheckAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub '${OrgName}-mis-r53-alarm'
      AlarmDescription: MIS Route 53 health check is failing
      Namespace: AWS/Route53
      MetricName: HealthCheckStatus
      Dimensions:
        - Name: HealthCheckId
          Value: !GetAtt MisHealthCheck.HealthCheckId
      Statistic: Minimum
      Period: 60
      EvaluationPeriods: !Ref HealthCheckFailureThreshold
      Threshold: 1
      ComparisonOperator: LessThanThreshold
      TreatMissingData: breaching
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

  MisCanaryLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/aws/synthetics/${OrgName}-mis'
      RetentionInDays: !Ref LogRetentionDays

  MisCanary:
    Type: AWS::Synthetics::Canary
    DependsOn: MisCanaryLogGroup
    Properties:
      Name: !Sub '${OrgName}-mis'
      ArtifactS3Location: !Sub 's3://${CanaryArtifactBucket}/${OrgName}/mis'
      ExecutionRoleArn: !GetAtt CanaryExecutionRole.Arn
      RuntimeVersion: syn-nodejs-puppeteer-9.1
      Schedule:
        Expression: !Ref CanarySchedule
      RunConfig:
        TimeoutInSeconds: 60
        EnvironmentVariables:
          ENDPOINT: !Ref MisEndpoint
          HEALTH_PATH: !Ref MisHealthCheckPath
      StartCanaryAfterCreation: true
      DeleteLambdaResourcesOnCanaryDeletion: true
      Code:
        Handler: index.handler
        Script: |
          const synthetics = require('Synthetics');
          exports.handler = async () => {
              await synthetics.executeHttpStep(
                  'Heartbeat',
                  'https://' + process.env.ENDPOINT + process.env.HEALTH_PATH
              );
          };
      Tags:
        - Key: Name
          Value: !Sub '${OrgName}-mis'
        - Key: Organization
          Value: !Ref OrgName
        - Key: Environment
          Value: !Ref EnvName

  MisCanaryAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub '${OrgName}-mis-canary-alarm'
      AlarmDescription: MIS Synthetics canary heartbeat failed
      Namespace: CloudWatchSynthetics
      MetricName: SuccessPercent
      Dimensions:
        - Name: CanaryName
          Value: !Ref MisCanary
      Statistic: Average
      Period: 300
      EvaluationPeriods: 1
      Threshold: 100
      ComparisonOperator: LessThanThreshold
      TreatMissingData: breaching
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

  MisCompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub '${OrgName}-mis-external-composite-alarm'
      AlarmDescription: MIS is unreachable — both Route 53 health check and Synthetics canary are failing
      AlarmRule: !Sub 'ALARM(${MisHealthCheckAlarm}) AND ALARM(${MisCanaryAlarm})'
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

##
# CICS
##

  CicsHealthCheck:
    Type: AWS::Route53::HealthCheck
    Properties:
      HealthCheckConfig:
        Type: HTTPS
        FullyQualifiedDomainName: !Ref CicsEndpoint
        ResourcePath: !Ref CicsHealthCheckPath
        RequestInterval: !Ref HealthCheckInterval
        FailureThreshold: !Ref HealthCheckFailureThreshold
        EnableSNI: true
      HealthCheckTags:
        - Key: Name
          Value: !Sub '${OrgName}-cics-health-check'
        - Key: Organization
          Value: !Ref OrgName
        - Key: Environment
          Value: !Ref EnvName

  CicsHealthCheckAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub '${OrgName}-cics-r53-alarm'
      AlarmDescription: CICS Route 53 health check is failing
      Namespace: AWS/Route53
      MetricName: HealthCheckStatus
      Dimensions:
        - Name: HealthCheckId
          Value: !GetAtt CicsHealthCheck.HealthCheckId
      Statistic: Minimum
      Period: 60
      EvaluationPeriods: !Ref HealthCheckFailureThreshold
      Threshold: 1
      ComparisonOperator: LessThanThreshold
      TreatMissingData: breaching
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

  CicsCanaryLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/aws/synthetics/${OrgName}-cics'
      RetentionInDays: !Ref LogRetentionDays

  CicsCanary:
    Type: AWS::Synthetics::Canary
    DependsOn: CicsCanaryLogGroup
    Properties:
      Name: !Sub '${OrgName}-cics'
      ArtifactS3Location: !Sub 's3://${CanaryArtifactBucket}/${OrgName}/cics'
      ExecutionRoleArn: !GetAtt CanaryExecutionRole.Arn
      RuntimeVersion: syn-nodejs-puppeteer-9.1
      Schedule:
        Expression: !Ref CanarySchedule
      RunConfig:
        TimeoutInSeconds: 60
        EnvironmentVariables:
          ENDPOINT: !Ref CicsEndpoint
          HEALTH_PATH: !Ref CicsHealthCheckPath
      StartCanaryAfterCreation: true
      DeleteLambdaResourcesOnCanaryDeletion: true
      Code:
        Handler: index.handler
        Script: |
          const synthetics = require('Synthetics');
          exports.handler = async () => {
              await synthetics.executeHttpStep(
                  'Heartbeat',
                  'https://' + process.env.ENDPOINT + process.env.HEALTH_PATH
              );
          };
      Tags:
        - Key: Name
          Value: !Sub '${OrgName}-cics'
        - Key: Organization
          Value: !Ref OrgName
        - Key: Environment
          Value: !Ref EnvName

  CicsCanaryAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub '${OrgName}-cics-canary-alarm'
      AlarmDescription: CICS Synthetics canary heartbeat failed
      Namespace: CloudWatchSynthetics
      MetricName: SuccessPercent
      Dimensions:
        - Name: CanaryName
          Value: !Ref CicsCanary
      Statistic: Average
      Period: 300
      EvaluationPeriods: 1
      Threshold: 100
      ComparisonOperator: LessThanThreshold
      TreatMissingData: breaching
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

  CicsCompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub '${OrgName}-cics-external-composite-alarm'
      AlarmDescription: CICS is unreachable — both Route 53 health check and Synthetics canary are failing
      AlarmRule: !Sub 'ALARM(${CicsHealthCheckAlarm}) AND ALARM(${CicsCanaryAlarm})'
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

##
# DirectDebit
##

  DirectDebitHealthCheck:
    Type: AWS::Route53::HealthCheck
    Properties:
      HealthCheckConfig:
        Type: HTTPS
        FullyQualifiedDomainName: !Ref DirectDebitEndpoint
        ResourcePath: !Ref DirectDebitHealthCheckPath
        RequestInterval: !Ref HealthCheckInterval
        FailureThreshold: !Ref HealthCheckFailureThreshold
        EnableSNI: true
      HealthCheckTags:
        - Key: Name
          Value: !Sub '${OrgName}-directdebit-health-check'
        - Key: Organization
          Value: !Ref OrgName
        - Key: Environment
          Value: !Ref EnvName

  DirectDebitHealthCheckAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub '${OrgName}-directdebit-r53-alarm'
      AlarmDescription: DirectDebit Route 53 health check is failing
      Namespace: AWS/Route53
      MetricName: HealthCheckStatus
      Dimensions:
        - Name: HealthCheckId
          Value: !GetAtt DirectDebitHealthCheck.HealthCheckId
      Statistic: Minimum
      Period: 60
      EvaluationPeriods: !Ref HealthCheckFailureThreshold
      Threshold: 1
      ComparisonOperator: LessThanThreshold
      TreatMissingData: breaching
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

  DirectDebitCanaryLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/aws/synthetics/${OrgName}-directdebit'
      RetentionInDays: !Ref LogRetentionDays

  DirectDebitCanary:
    Type: AWS::Synthetics::Canary
    DependsOn: DirectDebitCanaryLogGroup
    Properties:
      Name: !Sub '${OrgName}-directdebit'
      ArtifactS3Location: !Sub 's3://${CanaryArtifactBucket}/${OrgName}/directdebit'
      ExecutionRoleArn: !GetAtt CanaryExecutionRole.Arn
      RuntimeVersion: syn-nodejs-puppeteer-9.1
      Schedule:
        Expression: !Ref CanarySchedule
      RunConfig:
        TimeoutInSeconds: 60
        EnvironmentVariables:
          ENDPOINT: !Ref DirectDebitEndpoint
          HEALTH_PATH: !Ref DirectDebitHealthCheckPath
      StartCanaryAfterCreation: true
      DeleteLambdaResourcesOnCanaryDeletion: true
      Code:
        Handler: index.handler
        Script: |
          const synthetics = require('Synthetics');
          exports.handler = async () => {
              await synthetics.executeHttpStep(
                  'Heartbeat',
                  'https://' + process.env.ENDPOINT + process.env.HEALTH_PATH
              );
          };
      Tags:
        - Key: Name
          Value: !Sub '${OrgName}-directdebit'
        - Key: Organization
          Value: !Ref OrgName
        - Key: Environment
          Value: !Ref EnvName

  DirectDebitCanaryAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub '${OrgName}-directdebit-canary-alarm'
      AlarmDescription: DirectDebit Synthetics canary heartbeat failed
      Namespace: CloudWatchSynthetics
      MetricName: SuccessPercent
      Dimensions:
        - Name: CanaryName
          Value: !Ref DirectDebitCanary
      Statistic: Average
      Period: 300
      EvaluationPeriods: 1
      Threshold: 100
      ComparisonOperator: LessThanThreshold
      TreatMissingData: breaching
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

  DirectDebitCompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub '${OrgName}-directdebit-external-composite-alarm'
      AlarmDescription: DirectDebit is unreachable — both Route 53 health check and Synthetics canary are failing
      AlarmRule: !Sub 'ALARM(${DirectDebitHealthCheckAlarm}) AND ALARM(${DirectDebitCanaryAlarm})'
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

##
# PAS6
##

  Pas6HealthCheck:
    Type: AWS::Route53::HealthCheck
    Properties:
      HealthCheckConfig:
        Type: HTTPS
        FullyQualifiedDomainName: !Ref Pas6Endpoint
        ResourcePath: !Ref Pas6HealthCheckPath
        RequestInterval: !Ref HealthCheckInterval
        FailureThreshold: !Ref HealthCheckFailureThreshold
        EnableSNI: true
      HealthCheckTags:
        - Key: Name
          Value: !Sub '${OrgName}-pas6-health-check'
        - Key: Organization
          Value: !Ref OrgName
        - Key: Environment
          Value: !Ref EnvName

  Pas6HealthCheckAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub '${OrgName}-pas6-r53-alarm'
      AlarmDescription: PAS6 Route 53 health check is failing
      Namespace: AWS/Route53
      MetricName: HealthCheckStatus
      Dimensions:
        - Name: HealthCheckId
          Value: !GetAtt Pas6HealthCheck.HealthCheckId
      Statistic: Minimum
      Period: 60
      EvaluationPeriods: !Ref HealthCheckFailureThreshold
      Threshold: 1
      ComparisonOperator: LessThanThreshold
      TreatMissingData: breaching
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

  Pas6CanaryLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/aws/synthetics/${OrgName}-pas6'
      RetentionInDays: !Ref LogRetentionDays

  Pas6Canary:
    Type: AWS::Synthetics::Canary
    DependsOn: Pas6CanaryLogGroup
    Properties:
      Name: !Sub '${OrgName}-pas6'
      ArtifactS3Location: !Sub 's3://${CanaryArtifactBucket}/${OrgName}/pas6'
      ExecutionRoleArn: !GetAtt CanaryExecutionRole.Arn
      RuntimeVersion: syn-nodejs-puppeteer-9.1
      Schedule:
        Expression: !Ref CanarySchedule
      RunConfig:
        TimeoutInSeconds: 60
        EnvironmentVariables:
          ENDPOINT: !Ref Pas6Endpoint
          HEALTH_PATH: !Ref Pas6HealthCheckPath
      StartCanaryAfterCreation: true
      DeleteLambdaResourcesOnCanaryDeletion: true
      Code:
        Handler: index.handler
        Script: |
          const synthetics = require('Synthetics');
          exports.handler = async () => {
              await synthetics.executeHttpStep(
                  'Heartbeat',
                  'https://' + process.env.ENDPOINT + process.env.HEALTH_PATH
              );
          };
      Tags:
        - Key: Name
          Value: !Sub '${OrgName}-pas6'
        - Key: Organization
          Value: !Ref OrgName
        - Key: Environment
          Value: !Ref EnvName

  Pas6CanaryAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub '${OrgName}-pas6-canary-alarm'
      AlarmDescription: PAS6 Synthetics canary heartbeat failed
      Namespace: CloudWatchSynthetics
      MetricName: SuccessPercent
      Dimensions:
        - Name: CanaryName
          Value: !Ref Pas6Canary
      Statistic: Average
      Period: 300
      EvaluationPeriods: 1
      Threshold: 100
      ComparisonOperator: LessThanThreshold
      TreatMissingData: breaching
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

  Pas6CompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub '${OrgName}-pas6-external-composite-alarm'
      AlarmDescription: PAS6 is unreachable — both Route 53 health check and Synthetics canary are failing
      AlarmRule: !Sub 'ALARM(${Pas6HealthCheckAlarm}) AND ALARM(${Pas6CanaryAlarm})'
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

##
# DSAPP
##

  DsappHealthCheck:
    Type: AWS::Route53::HealthCheck
    Properties:
      HealthCheckConfig:
        Type: HTTPS
        FullyQualifiedDomainName: !Ref DsappEndpoint
        ResourcePath: !Ref DsappHealthCheckPath
        RequestInterval: !Ref HealthCheckInterval
        FailureThreshold: !Ref HealthCheckFailureThreshold
        EnableSNI: true
      HealthCheckTags:
        - Key: Name
          Value: !Sub '${OrgName}-dsapp-health-check'
        - Key: Organization
          Value: !Ref OrgName
        - Key: Environment
          Value: !Ref EnvName

  DsappHealthCheckAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub '${OrgName}-dsapp-r53-alarm'
      AlarmDescription: DSAPP Route 53 health check is failing
      Namespace: AWS/Route53
      MetricName: HealthCheckStatus
      Dimensions:
        - Name: HealthCheckId
          Value: !GetAtt DsappHealthCheck.HealthCheckId
      Statistic: Minimum
      Period: 60
      EvaluationPeriods: !Ref HealthCheckFailureThreshold
      Threshold: 1
      ComparisonOperator: LessThanThreshold
      TreatMissingData: breaching
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

  DsappCanaryLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/aws/synthetics/${OrgName}-dsapp'
      RetentionInDays: !Ref LogRetentionDays

  DsappCanary:
    Type: AWS::Synthetics::Canary
    DependsOn: DsappCanaryLogGroup
    Properties:
      Name: !Sub '${OrgName}-dsapp'
      ArtifactS3Location: !Sub 's3://${CanaryArtifactBucket}/${OrgName}/dsapp'
      ExecutionRoleArn: !GetAtt CanaryExecutionRole.Arn
      RuntimeVersion: syn-nodejs-puppeteer-9.1
      Schedule:
        Expression: !Ref CanarySchedule
      RunConfig:
        TimeoutInSeconds: 60
        EnvironmentVariables:
          ENDPOINT: !Ref DsappEndpoint
          HEALTH_PATH: !Ref DsappHealthCheckPath
      StartCanaryAfterCreation: true
      DeleteLambdaResourcesOnCanaryDeletion: true
      Code:
        Handler: index.handler
        Script: |
          const synthetics = require('Synthetics');
          exports.handler = async () => {
              await synthetics.executeHttpStep(
                  'Heartbeat',
                  'https://' + process.env.ENDPOINT + process.env.HEALTH_PATH
              );
          };
      Tags:
        - Key: Name
          Value: !Sub '${OrgName}-dsapp'
        - Key: Organization
          Value: !Ref OrgName
        - Key: Environment
          Value: !Ref EnvName

  DsappCanaryAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub '${OrgName}-dsapp-canary-alarm'
      AlarmDescription: DSAPP Synthetics canary heartbeat failed
      Namespace: CloudWatchSynthetics
      MetricName: SuccessPercent
      Dimensions:
        - Name: CanaryName
          Value: !Ref DsappCanary
      Statistic: Average
      Period: 300
      EvaluationPeriods: 1
      Threshold: 100
      ComparisonOperator: LessThanThreshold
      TreatMissingData: breaching
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

  DsappCompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub '${OrgName}-dsapp-external-composite-alarm'
      AlarmDescription: DSAPP is unreachable — both Route 53 health check and Synthetics canary are failing
      AlarmRule: !Sub 'ALARM(${DsappHealthCheckAlarm}) AND ALARM(${DsappCanaryAlarm})'
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

##
# PayGate
##

  PaygateHealthCheck:
    Type: AWS::Route53::HealthCheck
    Properties:
      HealthCheckConfig:
        Type: HTTPS
        FullyQualifiedDomainName: !Ref PaygateEndpoint
        ResourcePath: !Ref PaygateHealthCheckPath
        RequestInterval: !Ref HealthCheckInterval
        FailureThreshold: !Ref HealthCheckFailureThreshold
        EnableSNI: true
      HealthCheckTags:
        - Key: Name
          Value: !Sub '${OrgName}-paygate-health-check'
        - Key: Organization
          Value: !Ref OrgName
        - Key: Environment
          Value: !Ref EnvName

  PaygateHealthCheckAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub '${OrgName}-paygate-r53-alarm'
      AlarmDescription: PayGate Route 53 health check is failing
      Namespace: AWS/Route53
      MetricName: HealthCheckStatus
      Dimensions:
        - Name: HealthCheckId
          Value: !GetAtt PaygateHealthCheck.HealthCheckId
      Statistic: Minimum
      Period: 60
      EvaluationPeriods: !Ref HealthCheckFailureThreshold
      Threshold: 1
      ComparisonOperator: LessThanThreshold
      TreatMissingData: breaching
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

  PaygateCanaryLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/aws/synthetics/${OrgName}-paygate'
      RetentionInDays: !Ref LogRetentionDays

  PaygateCanary:
    Type: AWS::Synthetics::Canary
    DependsOn: PaygateCanaryLogGroup
    Properties:
      Name: !Sub '${OrgName}-paygate'
      ArtifactS3Location: !Sub 's3://${CanaryArtifactBucket}/${OrgName}/paygate'
      ExecutionRoleArn: !GetAtt CanaryExecutionRole.Arn
      RuntimeVersion: syn-nodejs-puppeteer-9.1
      Schedule:
        Expression: !Ref CanarySchedule
      RunConfig:
        TimeoutInSeconds: 60
        EnvironmentVariables:
          ENDPOINT: !Ref PaygateEndpoint
          HEALTH_PATH: !Ref PaygateHealthCheckPath
      StartCanaryAfterCreation: true
      DeleteLambdaResourcesOnCanaryDeletion: true
      Code:
        Handler: index.handler
        Script: |
          const synthetics = require('Synthetics');
          exports.handler = async () => {
              await synthetics.executeHttpStep(
                  'Heartbeat',
                  'https://' + process.env.ENDPOINT + process.env.HEALTH_PATH
              );
          };
      Tags:
        - Key: Name
          Value: !Sub '${OrgName}-paygate'
        - Key: Organization
          Value: !Ref OrgName
        - Key: Environment
          Value: !Ref EnvName

  PaygateCanaryAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub '${OrgName}-paygate-canary-alarm'
      AlarmDescription: PayGate Synthetics canary heartbeat failed
      Namespace: CloudWatchSynthetics
      MetricName: SuccessPercent
      Dimensions:
        - Name: CanaryName
          Value: !Ref PaygateCanary
      Statistic: Average
      Period: 300
      EvaluationPeriods: 1
      Threshold: 100
      ComparisonOperator: LessThanThreshold
      TreatMissingData: breaching
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

  PaygateCompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub '${OrgName}-paygate-external-composite-alarm'
      AlarmDescription: PayGate is unreachable — both Route 53 health check and Synthetics canary are failing
      AlarmRule: !Sub 'ALARM(${PaygateHealthCheckAlarm}) AND ALARM(${PaygateCanaryAlarm})'
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

##
# PhilPass
##

  PhilpassHealthCheck:
    Type: AWS::Route53::HealthCheck
    Properties:
      HealthCheckConfig:
        Type: HTTPS
        FullyQualifiedDomainName: !Ref PhilpassEndpoint
        ResourcePath: !Ref PhilpassHealthCheckPath
        RequestInterval: !Ref HealthCheckInterval
        FailureThreshold: !Ref HealthCheckFailureThreshold
        EnableSNI: true
      HealthCheckTags:
        - Key: Name
          Value: !Sub '${OrgName}-philpass-health-check'
        - Key: Organization
          Value: !Ref OrgName
        - Key: Environment
          Value: !Ref EnvName

  PhilpassHealthCheckAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub '${OrgName}-philpass-r53-alarm'
      AlarmDescription: PhilPass Route 53 health check is failing
      Namespace: AWS/Route53
      MetricName: HealthCheckStatus
      Dimensions:
        - Name: HealthCheckId
          Value: !GetAtt PhilpassHealthCheck.HealthCheckId
      Statistic: Minimum
      Period: 60
      EvaluationPeriods: !Ref HealthCheckFailureThreshold
      Threshold: 1
      ComparisonOperator: LessThanThreshold
      TreatMissingData: breaching
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

  PhilpassCanaryLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/aws/synthetics/${OrgName}-philpass'
      RetentionInDays: !Ref LogRetentionDays

  PhilpassCanary:
    Type: AWS::Synthetics::Canary
    DependsOn: PhilpassCanaryLogGroup
    Properties:
      Name: !Sub '${OrgName}-philpass'
      ArtifactS3Location: !Sub 's3://${CanaryArtifactBucket}/${OrgName}/philpass'
      ExecutionRoleArn: !GetAtt CanaryExecutionRole.Arn
      RuntimeVersion: syn-nodejs-puppeteer-9.1
      Schedule:
        Expression: !Ref CanarySchedule
      RunConfig:
        TimeoutInSeconds: 60
        EnvironmentVariables:
          ENDPOINT: !Ref PhilpassEndpoint
          HEALTH_PATH: !Ref PhilpassHealthCheckPath
      StartCanaryAfterCreation: true
      DeleteLambdaResourcesOnCanaryDeletion: true
      Code:
        Handler: index.handler
        Script: |
          const synthetics = require('Synthetics');
          exports.handler = async () => {
              await synthetics.executeHttpStep(
                  'Heartbeat',
                  'https://' + process.env.ENDPOINT + process.env.HEALTH_PATH
              );
          };
      Tags:
        - Key: Name
          Value: !Sub '${OrgName}-philpass'
        - Key: Organization
          Value: !Ref OrgName
        - Key: Environment
          Value: !Ref EnvName

  PhilpassCanaryAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub '${OrgName}-philpass-canary-alarm'
      AlarmDescription: PhilPass Synthetics canary heartbeat failed
      Namespace: CloudWatchSynthetics
      MetricName: SuccessPercent
      Dimensions:
        - Name: CanaryName
          Value: !Ref PhilpassCanary
      Statistic: Average
      Period: 300
      EvaluationPeriods: 1
      Threshold: 100
      ComparisonOperator: LessThanThreshold
      TreatMissingData: breaching
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'

  PhilpassCompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub '${OrgName}-philpass-external-composite-alarm'
      AlarmDescription: PhilPass is unreachable — both Route 53 health check and Synthetics canary are failing
      AlarmRule: !Sub 'ALARM(${PhilpassHealthCheckAlarm}) AND ALARM(${PhilpassCanaryAlarm})'
      AlarmActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
      OKActions:
        - Fn::ImportValue: !Sub '${SNSStackName}-TopicArn'
